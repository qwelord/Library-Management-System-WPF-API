<UserControl x:Class="recycler_view_Lukashevich.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:recycler_view_Lukashevich.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="250" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" KeyDown="SearchTextBox_KeyDown"/>
            <Button Content="🔍 Найти" Width="80" Click="SearchButton_Click"/>
            <Button Content="➕ Добавить" Width="80" Margin="5,0,0,0" Click="AddButton_Click"/>
            <Button Content="✏️ Редактировать" Width="100" Margin="5,0,0,0" Click="EditButton_Click"/>
            <Button Content="🗑️ Удалить" Width="80" Margin="5,0,0,0" Click="DeleteButton_Click"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  IsSynchronizedWithCurrentItem="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="ФИО" Width="150" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Email" Width="200" DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn Header="Роль" Width="80" DisplayMemberBinding="{Binding Role}"/>
                    <GridViewColumn Header="Дата регистрации" Width="150" DisplayMemberBinding="{Binding RegistrationDate}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>